describe('Array Hllo', function () {
    it('should same with javascript', function () {
        const name = ["Hello Affandi"];
        const values = [1, 2, 3];
        console.info(name, values);
    });
    it('should same reaadonly array', function () {
        let hobbies = ["Membaca", "Menulis"];
        console.log(hobbies);
    });
    it('should support tuplle', function () {
        let person = ["Affandi", "Affandi", "Affandi"];
        console.log(person);
    });
});
export {};
