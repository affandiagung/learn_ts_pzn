describe('Any', function () {
    it('should support in typescript', function () {
        const person = {
            name: "Affandi",
            age: 20
        };
        person.age = 30;
        console.info(person);
    });
});
export {};
