describe('Data Type', () => {
    it('should be string', () => {
        let name = 'Affandi';
        let balance = 100;
        let isVip = true;
        console.info(name, balance, isVip);
        // expect(typeof name).toBe('string')
    });
});
export {};
