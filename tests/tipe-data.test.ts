describe('Data Type', () => {
  it('should be string', () => {
    let name:string = 'Affandi'
    let balance :number = 100
    let isVip :boolean = true
    
    console.info(name, balance, isVip)
    // expect(typeof name).toBe('string')
  })
})
